server:
  port: 8081
spring:
  application:
    name: svb-dubbo-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1
        username: nacos
        password: nacos
        context-path: /nacos
        file-extension: yml
#        namespace: public
dubbo:
  application:
    name: ${spring.application.name}
    version: 1.0.0
  registry:
    address: nacos://127.0.0.1:8848
  protocol:
    name: dubbo
    port: -1
